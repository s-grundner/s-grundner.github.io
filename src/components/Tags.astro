---
interface Props {
	tags: string[];
}
const { tags } = Astro.props;
---

{
	// Only Display Tags if any exist
	tags && tags.length > 0 && (
		<nav>
			<h3 class="text-2xl">Tags</h3>
			<ul>
				{tags.map((tag) => (
					<li class="pillbox">{tag}</li>
				))}
			</ul>
		</nav>
	)
}

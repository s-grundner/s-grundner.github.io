---
import TOC from "../components/TOC.astro";
import "../styles/callout.css";

interface Props {
	headings: {
		depth: number;
		text: string;
		slug: string;
	}[];
}
const { headings } = Astro.props;
const show = false;
---

<article>
	{show && headings && headings.length > 0 && <TOC headings={headings} />}
	<div class="markdown-wrapper"><slot /></div>
</article>
